# Contributing Guidelines

- Follow the JSON schema in `schema/lexicon-schema.json`.
- Add one JSON file per headword under `lexicon/<first-letter>/`.
- Submit audio files to `audio/raw/` and update `audio/metadata.csv`.
- Use PRs. CI will validate entries automatically.
